*  `len(slice) == n`

*  `cap(slice) == m`

*  `len(array) == cap(array) == m`

!---
![Array versus slice](fig/array-vs-slice.png)\
![Array versus slice](fig/array-vs-slice.png)
!---
Figure: An array versus a slice.
